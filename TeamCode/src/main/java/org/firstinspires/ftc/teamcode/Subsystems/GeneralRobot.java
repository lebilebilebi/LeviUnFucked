package org.firstinspires.ftc.teamcode.Subsystems;

/**
 * @deprecated Use Shooter, Intake, and LLSub subsystems instead.
 * This class has been replaced by separate subsystem classes:
 * - Shooter: handles flywheel, hood, gate, LED
 * - Intake: handles intake motors with current limiting
 * - LLSub: handles Limelight and turret tracking
 *
 * See DECODEOPMODE1 for example usage of the new architecture.
 */
@Deprecated
public class GeneralRobot {
    // This class is deprecated and should not be used.
    // Keeping empty shell to prevent compilation errors in other files that may reference it.
}
